{
  "location": {
    "name": "Chico, CA",
    "latitude": 39.72849,
    "longitude": -121.83748,
    "timezone": "America/Los_Angeles"
  },
  "units": {
    "temperature": "fahrenheit",
    "wind": "mph",
    "precipitation": "inch",
    "clock24": true
  },
  "server": {
    "host": "127.0.0.1",
    "port": 4320
  },
  "control": {
    "_comment": "bin/dashd-control -- mobile-facing config surface (#30). Binds LAN-reachable (unlike server.host above), so every route past /api/health requires the pairing token; qr.svg/rotate are loopback-only regardless of token.",
    "host": "0.0.0.0",
    "port": 4321
  },
  "devices": {
    "_comment": "Networked-devices panel (#27), editable from the Control Backend. scan_target must match this machine's actual subnet (`ip -4 addr show`) -- it was previously left at a placeholder (192.168.1.0/24) that didn't match this network at all, so it was ping-sweeping a subnet the user isn't even part of. lib/devices.py now refuses to scan a target outside every local interface's subnet as a backstop, but keep this correct regardless.",
    "scan_interval_seconds": 300,
    "scan_target": "10.1.0.0/23"
  },
  "logs": {
    "_comment": "Log-highlighter panel (#28), source editable from the Control Backend. source_type is 'journalctl' or 'file'.",
    "source_type": "journalctl",
    "journalctl_unit": "ssh",
    "file_path": null,
    "max_lines": 200,
    "patterns": [
      {
        "regex": "Failed password|authentication failure|Invalid user",
        "status": "critical",
        "label": "Failed login"
      },
      {
        "regex": "Accepted password|Accepted publickey",
        "status": "ok",
        "label": "Login OK"
      },
      {
        "regex": "sudo:.*COMMAND",
        "status": "warn",
        "label": "sudo"
      }
    ]
  },
  "display": {
    "layer": "BOTTOM",
    "transparent": true,
    "theme": "retro_terminal",
    "_theme_options": [
      "night_ops",
      "retro_terminal"
    ],
    "safe_area_top": 48,
    "safe_area_bottom": 48
  },
  "refresh": {
    "weather_seconds": 600,
    "state_poll_seconds": 5,
    "metrics_sample_seconds": 30,
    "metrics_retain_hours": 24
  },
  "outputs": {
    "_comment": "Per-output layout overrides. Key = monitor model or connector as reported by the host (see `dashd-host --list`). 'layout' picks a top-level layout in web/app.js; omit an output to use 'auto'.",
    "DELL U2913WM": {
      "layout": "wide",
      "scale": 1.0
    },
    "default": {
      "layout": "auto",
      "scale": 1.0
    }
  }
}
